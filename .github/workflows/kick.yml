name: Kick

on:
  workflow_run:
    workflows: [pre-merge]
    types: [completed]

jobs:
  job-check:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run:  |
          echo 'Worked upon job completion!'
          echo '${{ toJSON(github.event.workflow_run) }}'